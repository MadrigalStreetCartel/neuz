export type FrontendInfoModel = {
    /* enemy_bounds?: Bounds[],
    active_enemy_bounds?: Bounds, */
    enemy_kill_count: number,
    is_attacking: boolean,
    is_running: boolean,
    is_alive: boolean,
    kill_min_avg: number,
    kill_hour_avg: number,
}
